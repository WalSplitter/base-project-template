{
  "name": "@base-template/web",
  "version": "1.0.0",
  "description": "Full-stack web application",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace frontend & npm run dev --workspace backend",
    "build": "npm run build --workspace frontend && npm run build --workspace backend",
    "test": "npm run test --workspace frontend && npm run test --workspace backend",
    "test:watch": "npm run test:watch --workspace frontend && npm run test:watch --workspace backend",
    "test:coverage": "npm run test:coverage --workspace frontend && npm run test:coverage --workspace backend",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace backend",
    "lint:fix": "npm run lint:fix --workspace frontend && npm run lint:fix --workspace backend",
    "type-check": "npm run type-check --workspace frontend && npm run type-check --workspace backend",
    "clean": "npm run clean --workspace frontend && npm run clean --workspace backend"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "keywords": [
    "web",
    "frontend",
    "backend",
    "typescript"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}